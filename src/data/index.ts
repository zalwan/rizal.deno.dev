import * as navbar from "@/data/navbarData";
import * as hero from "@/data/heroData";
import * as logo from "@/data/logoData";

const allData = {
  navbar,
  hero,
  logo,
};

export default allData;
